const tabs=[...document.querySelectorAll('.tabs button')];const options=[...document.querySelectorAll('.option')];const price=document.getElementById('price');const per=document.getElementById('per');const cartBtn=document.getElementById('demoCart');const toast=document.getElementById('toast');let mode='sub';let selected=options[0];function refresh(){const amount=Number(selected.dataset[mode]);const servings=Number(selected.dataset.servings);price.textContent=`$${amount}`;per.textContent=`$${(amount/servings).toFixed(2)}`;cartBtn.textContent=`Add to demo cart — $${amount}`;options.forEach(o=>{o.querySelector('strong').textContent=`$${o.dataset[mode]}`})}tabs.forEach(tab=>tab.addEventListener('click',()=>{tabs.forEach(t=>t.classList.remove('active'));tab.classList.add('active');mode=tab.dataset.mode;refresh()}));options.forEach(option=>option.addEventListener('click',()=>{options.forEach(o=>o.classList.remove('active'));option.classList.add('active');selected=option;refresh()}));cartBtn.addEventListener('click',()=>{toast.classList.add('show');setTimeout(()=>toast.classList.remove('show'),2200)});document.querySelectorAll('a[href^="#"]').forEach(a=>a.addEventListener('click',e=>{const target=document.querySelector(a.getAttribute('href'));if(target){e.preventDefault();target.scrollIntoView({behavior:'smooth',block:'start'})}}));
